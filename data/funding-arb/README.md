# Сырьё бота 1: ставки, открытый интерес, ценовой удар

На этих данных посчитано всё исследование фандинг-арбитража GMX V2 против Hyperliquid. Скрипты,
которые их читают, лежат в
[`botLab-desktop/scripts/funding-arb-study/`](../../botLab-desktop/scripts/funding-arb-study/);
там же индекс прогонов с результатами. В коде этот каталог называется `DATA` и приходит из
`paths.mjs`, переопределяется переменной `FA_STUDY_DATA`.

## Что внутри

| каталог | файлов | что | объём |
|---|---|---|---|
| `gmx-rate-snapshots/` | 63 | почасовые снимки ставки фандинга GMX прямо из первоисточника, год 2025-06-20..2026-06-20 | 17 МБ |
| `gmx-oi-snapshots/` | 63 | почасовой открытый интерес по сторонам и база фандинга. Из него считается разбавление входа | 13 МБ |
| `spread-cache-y2/` | 23 | второй год ставок пары (2023-09..2025-06) для проверки на другом периоде | 6.0 МБ |
| `gmx-oi-snapshots-y2/` | 23 | базы фандинга к тому же второму году. Выкачаны 2026-08-30, см. предупреждение ниже | 8.2 МБ |
| `gmx-impact/` | 1 | кривая ценового удара GMX по корзинам размера, знак сохранён | 448 КБ |
| `hl/` | 3 | проскальзывание по стакану Hyperliquid и суточные обороты по 63 именам | 372 КБ |
| `gmx-trades/` | 20 | отметки времени исполненных ордеров на самых залипающих рынках | 340 КБ |
| `snapshots/` | 5 | ёмкость на 2026-08-30: свободная ликвидность рынков GMX, открытый интерес и оборот Hyperliquid | 232 КБ |

JSON и CSV хранятся сжатыми, кроме мелких снимков ёмкости.

## Как это читают скрипты

Прямо в этом виде скрипты каталог не читают: они писались на плоские имена рабочего каталога и
на несжатые файлы. Соответствие имён такое:

| здесь | плоское имя, которое ждёт скрипт | сколько скриптов ждёт |
|---|---|---|
| `gmx-oi-snapshots/<токен>.json.gz` | `truth-a-oi2/<токен>.json` | 33 |
| `gmx-rate-snapshots/<токен>.json.gz` | `truth-a-src/<токен>.json` | 23 |
| `spread-cache-y2/<токен>.csv.gz` | `y2/<токен>.csv` | 7 |
| `gmx-impact/impact-gmx.json.gz`, `hl/*.json.gz` | `imp-raw/`, `impact-hl.json`, `vol63.json` | 12 |
| `gmx-trades/<токен>.json.gz` | `truth-a-trades/<токен>.json` | 3 |
| `snapshots/*.json` | те же имена в корне рабочего каталога | 5 |

Поэтому прогон с нуля требует сначала разложить данные обратно в плоский вид и распаковать, либо
перекачать их качающими скриптами заново. Причина, по которой так оставлено, и что из этого следует
для чисел исследования, разобраны в [README скриптов](../../botLab-desktop/scripts/funding-arb-study/README.md), раздел
«Как запускать».

## Предупреждение к `gmx-oi-snapshots-y2/`: это не летопись

Каталог выкачан **2026-08-30** запросом к индексатору за период **2023-09-25 .. 2025-06-20**, то есть
данные прошли через все переиндексации первоисточника и НЕ равны тому, что было бы снято тогда.
Измеренная цена такого дрейфа на другом периоде: за 71 день 40.4% часов сдвинулись на младший бит, а
4.88% записей стали отдаваться нулями при живом рынке с двусторонним открытым интересом.

Выгрузка поэтому не берётся на веру, а проверяется тождеством GMX
`|f_long| * B_long = |f_short| * B_short`, причём ставки для проверки приходят из другого источника
(`spread-cache-y2/`, собран раньше и сторонним проектом). Результат проверки
(`botLab-desktop/scripts/funding-arb-study/y2-2-verify.mjs`, 246 545 часов):

| | |
|---|---|
| покрытие ставок базой | 100.00% на всех 23 именах |
| нулевых баз | 0.28% (хуже всего FIL 5.38%, BCH 4.09%, TAO 2.03%) |
| невязка тождества | медиана 0.00e+0%, p99 3.70e-14%, макс 3.68e+0% |
| часов, где тождество не сошлось по существу | 0 |
| часов, где ставка на пределе разрядности источника | 14 (все ARB, 2024-12-21/22, обе ставки равны 1e-30) |
| флаг `useOpenInterestInTokensForBalance` | false во всех часах периода |

Последняя строка полезна отдельно: первый год лежит целиком в эпохе флага `true` (переключение
2025-12-22), а второй целиком в эпохе `false`. То есть два периода покрывают ОБА режима расчёта базы,
и это единственные данные проекта, на которых режим `false` вообще представлен.

Часы на пределе разрядности леджер отвергает кодом `base_identity_broken`. Это не ошибка данных:
денег в таком часе порядка 1e-21 доллара. Но причина отказа читается как «база пришла не та», хотя
верное объяснение «ставка ниже разрешения источника».

## Чего здесь нет

- **Кэш почасовых ставок за первый год** (93 файла `<токен>_1750402800_1781938800.csv`,
  2025-06-20..2026-06-20, 83 МБ). Он собран сторонним проектом и живёт вне репозитория; путь
  задаётся переменной `FA_SPREAD_CACHE`.
- **Сырые выгрузки прогонов** целиком: фактически уплаченный фандинг по позициям, полная лента
  ордеров GMX, калибровочные окна. Это около 750 МБ на прогон, и оно воспроизводимо: качается
  заново теми же скриптами из публичных источников (GraphQL GMX на Arbitrum и API Hyperliquid).
  Здесь оставлено то, без чего числа исследования не пересчитать.

## Дополнение от 2026-08-30: прогоны 6-9

После первого переноса исследование продолжилось ещё четырьмя прогонами (разбавление и переворот
сторон, оптимальный размер входа, керри перпа Hyperliquid, базис и маржа Hyperliquid). Их сырьё и
выгрузки добавлены сюда же:

| каталог | что | файлов |
|---|---|---|
| `hl-candles/` | свечи обеих книг Hyperliquid (перп и спот) по годным парам, все пять доступных шагов | 90 |
| `derived/` | выгрузки, которые прогоны считали и сохраняли: кривые, сводки, промежуточные срезы | 112 |

`derived/` это НЕ сырьё бирж, а посчитанные величины. Хранятся, потому что часть из них стоит
десятков минут запросов (например `truth-v-oi.json` это 26 МБ почасовых снимков открытого интереса,
собранных по 63 рынкам), и потому что по ним воспроизводятся числа отчётов без повторного похода в
сеть.

### Предел свечного крана Hyperliquid, найденный опытом

`candleSnapshot` отдаёт не более **5000 свечей за запрос**, и глубина хранения равна 5000 умножить
на шаг: 1m это 3.47 суток, 5m это 17.4, 15m это 52.1, 1h это 208.3, 1d это вся жизнь пары. Окно,
целиком лежащее за горизонтом хранения, возвращает ноль строк. Это ограничивает любой замер базиса
на мелком шаге двумя-тремя сутками истории, и переснять их задним числом уже нельзя. Поэтому
`hl-candles/` в репозитории: через неделю эти минутные ряды не воспроизводимы в принципе.
