# dependencies & build output (per-package, but ignored repo-wide for safety)
node_modules/
release/
dist/

# logs & OS cruft
*.log
.DS_Store

# runtime data caches are written to the OS userData dir, never the repo

# внутренние материалы исследований: письма заказчику, промпты, отчёты.
# Живут вне репозитория в ~/.claude/botLab/research/ и в публичный git не попадают.
research/

# локальные бэкапы машин (снимки профилей и деревьев), в репозиторий не входят
.backup/
